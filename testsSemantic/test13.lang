init
	a, b, c, maior is integer;
	
		read(a);
		read(b);
		read(c);
		maior := 0;
		if ( a>b ) //if ( a>b or a>c )
			begin				
				maior := a;
			end
		else
			begin
				if (a>c )
				begin				
					maior := a;
				end;
			end;
		


		write(“Maior idade: ”);
		write(maior);
stop